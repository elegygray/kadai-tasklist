<h1>id: <%= @task.id %>のタスク記録ページ</h1>
<table class = "table table-striped">
  <tr>
    <th>id</th>
    <th><%= @task.id%></th>
  </tr>
  <tr>
    <th>ステータス</th>
    <th><%= @task.status %></th>
  </tr>
  <tr>
    <th>タスク</th>
    <th><%= @task.content%></th>
  </tr>
</table>

<%= link_to '記録を編集する', edit_task_path(@task), class: "btn btn-default" %>
<%= link_to '記録を削除する', @task, method: :delete, data: { confirm: '本当に削除してよろしいですか？' }, class: "btn btn-danger" %>